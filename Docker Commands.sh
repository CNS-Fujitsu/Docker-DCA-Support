#!/bin/bash

#Get the client version and engine version of Docker
docker version

#Get the configuration information for a docker engine
docker info

#Get a list of all the main docker commands
docker

#Get a list of all the docker management commands
docker container
docker config
docker image









